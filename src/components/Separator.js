export const Separator = (props) => {
  return (
    <div class="separator_blue" id={`${props.id}`}>
      <div class="separator_in">
        <div class="separator_in2"></div>
      </div>
    </div>
  );
};
